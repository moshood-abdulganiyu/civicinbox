# civicinbox
AI-assisted request-triage system that classifies incoming messages, extracts structured details, and drafts reviewable responses. Compares a TF-IDF/logistic-regression baseline against an LLM-based classifier with evaluation on a labeled dataset.
